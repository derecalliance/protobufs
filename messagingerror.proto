syntax = "proto3";
import "result.proto";

package org.derecalliance.derec.protobuf;

/* 
 * Protocol message sent by a message receiver who receives a bad message back to the message sender.
 */
message MessagingErrorMessage {
    Result result = 1;
}

